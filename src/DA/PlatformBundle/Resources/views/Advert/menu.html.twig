<nav id="nav">
    <ul class="nav nav-pills nav-stacked">
        {% for advert in listAdverts %}
            <li>
                <a href="{{ path('da_platform_view', {'id': advert.id}) }}">
                    <span class="fa fa-check"></span>  {{ advert.title }}
                </a>
            </li>
        {% endfor %}
    </ul>
</nav>